{
  "_comment": "Example configuration for Claude Desktop with custom settings",
  "mcpServers": {
    "ui-test-assistant": {
      "command": "uv",
      "args": [
        "run",
        "python",
        "ui_test_server.py"
      ],
      "cwd": "ui_tests/claude_subagent",
      "env": {
        "PYTHONPATH": ".",
        "BASE_URL": "http://localhost:8001",
        "_comment_BASE_URL": "Change this to match your Admin UI port (e.g., http://localhost:8042 for conductor workspaces)",
        "HEADLESS": "true",
        "_comment_HEADLESS": "Set to false to see browser during test execution"
      }
    }
  }
}